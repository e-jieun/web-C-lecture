function page(idValue) {
  return `
    <div id="${idValue}">
      mainsection part
    
    </div>
  `;
};
/* 
  work .10 page() 함수
  ? 내보내기 처리된 page() 함수는 특별한 기능은 존재하지 않고,
  ? 단순히 매개변수를 통해 id 부분을 지정해줄 수 있는 기능'만' 가지고 있다.
  ? main.js 맨 윗줄 참조



*/
export default page;